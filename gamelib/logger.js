/**
 * An extremely simple logger. Literally just prints the input.
 *
 * @method log
 * @param message The message to be printed.
 * @for logger
 * @static
 */

function log(message) {
    console.log(message);
}

module.exports = log;
